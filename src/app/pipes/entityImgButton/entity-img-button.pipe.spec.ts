import { EntityImgButtonPipe } from './entity-img-button.pipe';

describe('EntityImgButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new EntityImgButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
